UI BUILD ORDER (Based on repository audit + user flow + broken/missing priorities)
1. SPLASH → INTRO → ONBOARDING (entry flow; missing onboarding interest selection)
2. AUTH SCREENS: Login, SignupStep1/2, ForgotPassword, ResetPassword, VerifyEmail, OtpVerification (security fixes needed: auth token storage, verification session)
3. PROFILE SETUP: SetupProfile (critical profile screen issues)
4. HOME / FEED: HomeScreen + PostComposer + PostCard + ReelsFeed + Stories + Trending (core feed broken; dailyStats unbounded; no ranking real data)
5. PROFILE SYSTEM: ProfileScreen / ProfilePublic / ProfileMy / ProfileSettings / EditProfile / ProfileAnalytics / CreatorDashboard (audit shows profile analytics, analytics store, profile store all have issues; profile screens rebuilt already but need full polish)
6. MESSAGING: MessagingScreen + ConversationSettings + GroupInfoScreen + NewConversation + messaging components (group info broken; offline sync needed; messaging store has lock/timeout issues)
7. NOTIFICATIONS: NotificationsScreen (no rate limit; no delivery; audit critical)
8. SEARCH / DISCOVERY: SearchScreen + SearchResults / Suggestions / Trending (search index broken; vector search mock; no rate limit)
9. CREATOR / MONETIZATION: CoinsScreen + GiftScreen + CreatorPayoutScreen + VideoAnalyticsScreen (monetization audit critical: Stripe webhook, adCache, ledger)
10. ADMIN / MODERATION / TRUST: AdminDashboard + ModerationQueue + UserManagement + ContentManagement (moderation pipeline needed; trust & safety)
11. SETTINGS / PRIVACY: SettingsScreen + Privacy-related (GDPR/compliance missing; audit trails needed)
12. OFFLINE / RELIABILITY: OfflineStatusScreen + sync indicators (offline architecture needed; queue processing)
ORDER JUSTIFICATION: User enters through splash/onboarding → must authenticate (security priority) → must set up profile (audit priority) → core experience is feed (scalability/performance priority) → profile (analytics/store fixes) → messaging (group/offline fixes) → notifications (spam/security) → search (index/scalability) → creator economy (financial/security) → admin (trust) → settings (compliance) → offline (reliability).
