/**
 * 🎨 POST CARD COMPONENT - REUSABLE
 * 📱 Handles ALL post types: text, image, video, audio, poll, question, link, event
 */

// This component is now embedded in HomeScreen.jsx for single-file approach
// Export it separately if needed for other screens

export { PostCard } from '../screens/HomeScreen.jsx';
